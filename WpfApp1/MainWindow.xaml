<Window x:Class="SmoothDrive.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
<<<<<<< HEAD
        Title="Smooth Drive" Height="1024" Width="1440"
        WindowStartupLocation="CenterScreen"
        Background="Black">
=======
        Title="Smooth Drive" Height="720" Width="1280"
        WindowStyle="None" WindowStartupLocation="CenterScreen" Background="Black">
>>>>>>> ffb02de49151eace75965c98d6e5603b3272191f

    <Grid>
        <!-- Заменили изображение на чёрный фон -->
        <Rectangle Fill="Black"/>

        <!-- Красный слой внизу -->
        <Rectangle Fill="#408B0000" VerticalAlignment="Bottom" Height="150"/>

        <!-- Логотип -->
        <StackPanel HorizontalAlignment="Left" VerticalAlignment="Top" Margin="50,50,0,0">
            <TextBlock Text="Smooth Drive" FontSize="64" Foreground="White" FontWeight="Bold" />
            <TextBlock Text="avtoschool" FontSize="24" Foreground="#FF8B0000" FontStyle="Italic" Margin="0,5,0,0"/>
        </StackPanel>

        <!-- Блок авторизации -->
        <Border Background="#CC333333" CornerRadius="15" Padding="20"
                Width="300" Height="200"
                HorizontalAlignment="Left" VerticalAlignment="Center" Margin="50,0,0,0">

            <StackPanel>
                <TextBlock Text="Авторизация" FontSize="18" Foreground="White" HorizontalAlignment="Center" Margin="0,0,0,15"/>

                <!-- Поле Логин -->
                <TextBox Margin="0,0,0,10" 
                         Background="White" Foreground="Black" 
                         Padding="10" BorderBrush="Gray" BorderThickness="1">
                    <TextBox.Style>
                        <Style TargetType="TextBox">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="TextBox">
                                        <Border Name="border" BorderBrush="{TemplateBinding BorderBrush}" 
                                                BorderThickness="{TemplateBinding BorderThickness}" 
                                                Background="{TemplateBinding Background}" 
                                                SnapsToDevicePixels="True">
                                            <ScrollViewer x:Name="PART_ContentHost" Focusable="False" />
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </TextBox.Style>
                </TextBox>

                <!-- Поле Пароль -->
                <PasswordBox Margin="0,0,0,15" 
                             Background="White" Foreground="Black" 
                             Padding="10" BorderBrush="Gray" BorderThickness="1">
                    <PasswordBox.Style>
                        <Style TargetType="PasswordBox">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="PasswordBox">
                                        <Border Name="border" BorderBrush="{TemplateBinding BorderBrush}" 
                                                BorderThickness="{TemplateBinding BorderThickness}" 
                                                Background="{TemplateBinding Background}" 
                                                SnapsToDevicePixels="True">
                                            <ScrollViewer x:Name="PART_ContentHost" Focusable="False" />
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </PasswordBox.Style>
                </PasswordBox>

                <!-- Кнопка Войти -->
                <Button Content="Войти" 
                        Background="#FF8B0000" Foreground="White" 
                        Padding="10,5" Margin="0,0,0,5"
                        HorizontalAlignment="Center" Width="80">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border Name="border" Background="{TemplateBinding Background}" 
                                    CornerRadius="10" Padding="{TemplateBinding Padding}">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                        </ControlTemplate>
                    </Button.Template>
                </Button>

                <!-- Ссылка "Забыли пароль?" -->
                <TextBlock Text="Забыли пароль?" FontSize="12" Foreground="LightGray" 
                           HorizontalAlignment="Right" Cursor="Hand" 
                           TextDecorations="Underline"/>
            </StackPanel>
        </Border>

        <!-- Логотип в левом верхнем углу -->
        <StackPanel HorizontalAlignment="Left" VerticalAlignment="Top" Margin="40,30,0,0"/>

        <!-- Панель авторизации — слева, поверх фона -->
        <Border Background="#80FFFFFF" CornerRadius="15" Padding="20"
                HorizontalAlignment="Center" VerticalAlignment="Top"
                Margin="0,449,0,0" Width="483" Height="380">
            <StackPanel>
                <!-- Заголовок "Авторизация" -->
                <TextBlock Text="Авторизация" 
                           FontSize="32" 
                           FontWeight="SemiBold"
                           HorizontalAlignment="Center" 
                           Margin="0,0,0,20"/>

                <!-- Поле Логин -->
                <StackPanel Margin="0,0,0,15">
                    <TextBlock Text="Логин"
                               FontSize="14"
                               Foreground="#666666"
                               Margin="5,0,0,5"/>
                    <Border Background="White"
                            CornerRadius="8">
                        <TextBox x:Name="LoginBox"
                                 Text=""
                                 Background="Transparent"
                                 Foreground="Black"
                                 BorderThickness="0"
                                 Padding="15,12"
                                 FontSize="18"/>
                    </Border>
                </StackPanel>

                <!-- Поле Пароль -->
                <StackPanel Margin="0,0,0,25">
                    <TextBlock Text="Пароль"
                               FontSize="14"
                               Foreground="#666666"
                               Margin="5,0,0,5"/>
                    <Border Background="White"
                            CornerRadius="8">
                        <PasswordBox x:Name="PasswordBox"
                                     Background="Transparent"
                                     Foreground="Black"
                                     BorderThickness="0"
                                     Padding="15,12"
                                     FontSize="18"/>
                    </Border>
                </StackPanel>

                <!-- Кнопка Войти -->
                <Button x:Name="LoginButton" 
                        Content="Войти"
                        Background="#FF651111" 
                        Foreground="White" 
                        BorderThickness="0"
                        FontSize="18"
                        FontWeight="SemiBold"
                        Height="50"
                        Margin="0,0,0,10"
                        Cursor="Hand"
                        Click="LoginButton_Click">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border Background="{TemplateBinding Background}"
                                    CornerRadius="8"
                                    Padding="15,0">
                                <ContentPresenter HorizontalAlignment="Center" 
                                                  VerticalAlignment="Center"/>
                            </Border>
                        </ControlTemplate>
                    </Button.Template>
                </Button>

                <!-- Ссылка "Забыли пароль?" -->
                <TextBlock Text="Забыли пароль?" 
                           FontSize="12" 
                           Foreground="Black"
                           HorizontalAlignment="Right" 
                           Margin="0,0,0,10"
                           TextDecorations="Underline" 
                           Cursor="Hand"/>

                <!-- Разделитель -->
                <Border Height="1" 
                        Background="#CCCCCC" 
                        Margin="0,0,0,15"/>

                <!-- Кнопка для регистрации -->
                <StackPanel HorizontalAlignment="Center">
                    <TextBlock Text="Нет профиля?" 
                               FontSize="14" 
                               Foreground="#666666"
                               Margin="0,0,0,5"/>

                    <Button x:Name="RegisterButton"
                            Content="Зарегистрироваться"
                            Background="Transparent"
                            Foreground="#2196F3"
                            BorderThickness="0"
                            FontSize="14"
                            FontWeight="SemiBold"
                            Padding="10,5"
                            Cursor="Hand">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border Background="Transparent"
                                        Padding="{TemplateBinding Padding}">
                                    <ContentPresenter HorizontalAlignment="Center" 
                                                      VerticalAlignment="Center"/>
                                </Border>
                            </ControlTemplate>
                        </Button.Template>
                    </Button>
                </StackPanel>
            </StackPanel>
        </Border>
        <TextBlock Text="Smooth Drive" FontSize="137" FontWeight="Bold" Foreground="White" Margin="277,30,232,796"/>
        <Border Margin="550,212,512,661">
            <TextBlock Text="avtoschool" FontSize="74" Foreground="#FF651111" FontStyle="Italic" Margin="0,0,-30,-46"/>
        </Border>
    </Grid>
<<<<<<< HEAD
</Window>
=======
</Window>
    
>>>>>>> ffb02de49151eace75965c98d6e5603b3272191f
